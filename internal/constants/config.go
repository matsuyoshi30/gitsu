package constants

const (
	// ConfigDir defines the name of the gitsu subfolder inside the user's config directory
	ConfigDir string = "gitsu-go"

	// ConfigFileName defines the name of the config file name
	ConfigFileName string = "config.json"

	// JsonIndent defines the indentation characters used when marshaling JSON data into a file
	JsonIndent string = "  "
)
